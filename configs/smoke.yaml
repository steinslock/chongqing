extends: default.yaml

smoke:
  limit_subjects: 32
  test_fraction: 0.25

features:
  allowed_smoke_columns:
    - has_EEG
    - has_fNIRS
    - has_face
    - has_eye_direct
    - has_eye_name_mapped
    - modality_count_direct
    - modality_count_with_eye_name_map

