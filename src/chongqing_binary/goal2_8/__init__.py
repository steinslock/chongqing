"""Goal 2.8: event-semantics recovery and feature re-derivation.

Task structure comes from `chongqing_binary.paradigm`, which is sourced from the
dataset attachments. Modelling machinery is reused from `goal2_7` rather than
cloned.
"""
